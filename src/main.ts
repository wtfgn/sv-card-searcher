import { createApp } from 'vue';
import './style.css';
import App from './App.vue';

createApp(App).mount('#app');

const x = 5;
console.log(x);
const y = {
    name: 'John',
    age: 30,
};
console.log(y);
